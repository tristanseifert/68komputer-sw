/*
 * Template file for the version labeling for the ROM. It's processed with the git tag/commit
 * during the start of build.
 */
#include "runtime/version.h"

const char * const gVersionCommit = "@VERSION_COMMIT@";
const char * const gVersionShort = "@VERSION_TAG@";
